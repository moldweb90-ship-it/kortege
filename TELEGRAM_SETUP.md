# 📱 Настройка отправки заявок в Telegram

## Шаг 1: Создание бота в Telegram

1. Открой Telegram и найди бота **@BotFather**
2. Напиши ему команду: `/newbot`
3. Бот попросит ввести имя бота (например: `KORTEGE Orders`)
4. Потом попросит ввести username бота (должен заканчиваться на `bot`, например: `kortege_orders_bot`)
5. **BotFather даст тебе ТОКЕН** — скопируй его и сохрани!
   - Пример токена: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Получение CHAT_ID (ID твоего Telegram) ⚠️ ВАЖНО!

**CHAT_ID — это твой ЛИЧНЫЙ Telegram ID, а НЕ ID бота!**

### Вариант 1: Через специального бота (проще всего) ✅

1. Найди бота **@userinfobot** в Telegram
2. Напиши ему любое сообщение
3. Он ответит твоим **ID** — это и есть твой CHAT_ID (число, например: `123456789`)
4. Скопируй это число

### Вариант 2: Через API (альтернативный способ)

1. Напиши своему новому боту любое сообщение (можно даже `/start`)
2. Открой в браузере: `https://api.telegram.org/bot<ТВОЙ_ТОКЕН>/getUpdates`
   - Замени `<ТВОЙ_ТОКЕН>` на токен от BotFather
   - Пример: `https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz/getUpdates`
3. Найди в ответе поле `"chat":{"id":123456789}` — это и есть твой CHAT_ID

### Вариант 3: Через helper endpoint (после деплоя) 🔧

После того как задеплоишь проект на Vercel:
1. Перейди на: `https://твой-домен.vercel.app/api/get-chat-id`
2. Сначала напиши своему боту любое сообщение (например `/start`)
3. Обнови страницу `/api/get-chat-id`
4. Там увидишь свой CHAT_ID

## Шаг 3: Добавление переменных на Vercel

1. Зайди на **Vercel.com** → свой проект
2. Перейди в **Settings** → **Environment Variables**
3. Добавь две переменные:

   **Переменная 1:**
   - Name: `BOT_TOKEN`
   - Value: `твой_токен_от_BotFather`
   - Environment: выбери все (Production, Preview, Development)

   **Переменная 2:**
   - Name: `CHAT_ID`
   - Value: `твой_chat_id`
   - Environment: выбери все (Production, Preview, Development)

4. Нажми **Save** для каждой переменной

## Шаг 4: Передеплой проекта

После добавления переменных нужно пересобрать проект:

1. В Vercel перейди во вкладку **Deployments**
2. Нажми на три точки (⋮) у последнего деплоя
3. Выбери **Redeploy**
4. Или можно сделать новый коммит и пуш в GitHub — Vercel автоматически пересоберет

## Шаг 5: Проверка

1. Зайди на свой сайт
2. Заполни форму заявки
3. Отправь
4. Проверь Telegram — должно прийти сообщение с данными заявки

## 🔧 Что уже настроено в коде:

✅ API функция создана: `/api/telegram.js`
✅ Форма обновлена для отправки данных
✅ Обработка ошибок добавлена
✅ Индикатор загрузки работает

## ❗ Важно:

- Токен и CHAT_ID хранятся только в переменных окружения Vercel (безопасно)
- После добавления переменных обязательно сделай Redeploy
- Бот должен быть активным (напиши ему `/start` хотя бы раз)

---

## Пример значений:

```
BOT_TOKEN = 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
CHAT_ID = 123456789
```

После настройки все заявки будут приходить в Telegram! 🚀

